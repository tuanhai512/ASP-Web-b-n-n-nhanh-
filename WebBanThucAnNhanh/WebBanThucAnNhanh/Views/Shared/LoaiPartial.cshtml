@using WebBanThucAnNhanh.Models
@model List<LOAI>
    <style>
       

        .div-cate {
           font-weight: 500;
    text-transform: uppercase;
        }
    </style>
    <ul style="list-style-type:none" >
        @{
        foreach (var item in Model)
        {
        <li class="div-cate">
            @Html.ActionLink(item.TENLOAI, "Index", "Home", new {loai=item.MALOAI.Trim()},"")
        </li>

        }
        }
    </ul>
